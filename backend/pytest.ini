[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    requires_db: needs a live PostgreSQL (DATABASE_URL); auto-applied to tests using the `client` fixture, skipped without a database
