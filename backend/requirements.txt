# FastAPI & server (pinned for reproducibility)
fastapi>=0.116.0
uvicorn[standard]>=0.32.1

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Pydantic (pydantic-ai 1.77.0 requires pydantic>=2.12)
pydantic>=2.12.0,<3.0.0
pydantic-settings>=2.6.1,<3.0.0

# Storage (MinIO/S3 compatible)
boto3>=1.42.14
minio>=7.2.14

# Async tasks (Celery + Redis)
celery[redis]>=5.4.0
redis>=5.2.1
psycopg2-binary>=2.9.10

# Document processing (Phase 1)
python-multipart>=0.0.17
pymupdf>=1.24.14
pymupdf4llm>=0.0.17  # layout-/table-preserving PDF → Markdown extraction
python-docx>=1.1.2
openpyxl>=3.1.5
python-pptx>=1.0.2

# AI / LLM
pydantic-ai==1.77.0
pydantic-evals==1.77.0  # extraction/check quality evaluation harness
openai>=1.61.0
ollama>=0.4.1
anthropic>=0.40.0

# Vector DB (Weaviate, optional for RAG checks)
weaviate-client>=4.9.2

# Data (YAML)
pyyaml>=6.0.2

# Auth (OIDC/JWT)
PyJWT[crypto]>=2.10.1
cryptography>=44.0.0

# Rate limiting
slowapi>=0.1.9

# Observability: Prometheus metrics + OpenTelemetry tracing
prometheus-client>=0.21.0
opentelemetry-sdk>=1.28.0
opentelemetry-instrumentation-fastapi>=0.49b0
opentelemetry-instrumentation-sqlalchemy>=0.49b0
# Optional OTLP exporter — only needed when OTEL_EXPORTER_OTLP_ENDPOINT is configured:
# opentelemetry-exporter-otlp-proto-http>=1.28.0

# Structured JSON logging
python-json-logger>=3.3.0

# Dev & testing
python-dotenv>=1.0.1
pytest>=8.3.4
pytest-asyncio>=0.24.0
httpx>=0.27.0
