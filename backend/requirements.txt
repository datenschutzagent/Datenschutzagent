# FastAPI & server (pinned for reproducibility)
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Pydantic
pydantic==2.10.3
pydantic-settings==2.6.1

# Storage (MinIO/S3 compatible)
boto3==1.35.70
minio==7.2.14

# Async tasks (Celery + Redis)
celery[redis]==5.4.0
redis==5.2.1
psycopg2-binary==2.9.10

# Document processing (Phase 1)
python-multipart==0.0.17
pymupdf==1.24.14
python-docx==1.1.2
openpyxl==3.1.5

# AI / LLM
pydantic-ai==1.77.0
openai>=1.61.0
ollama==0.4.1

# Vector DB (Weaviate, optional for RAG checks)
weaviate-client==4.9.2

# Data (YAML)
pyyaml==6.0.2

# Auth (OIDC/JWT)
PyJWT[crypto]==2.10.1
cryptography==44.0.0

# Rate limiting
slowapi==0.1.9

# Structured JSON logging
python-json-logger==3.3.0

# Dev & testing
python-dotenv==1.0.1
pytest==8.3.4
pytest-asyncio==0.24.0
# Pin auf 0.27.x: ollama 0.4.1 benötigt httpx<0.28.0
httpx>=0.27.0,<0.28.0
